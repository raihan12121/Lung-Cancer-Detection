fastapi==0.95.2
uvicorn[standard]==0.22.0
bcrypt==4.1.3
python-multipart==0.0.6
motor
pydantic[email]==1.10.12
numpy==1.26.4
pillow==10.1.0
torch==2.2.0
onnxruntime==1.15.1
# TensorFlow is platform-specific and large; on Windows this often fails to install.
# If you need TFLite support, either install a compatible `tflite-runtime` wheel for
# your Python/OS, install TensorFlow manually, or run the backend in a Linux Docker container.
# tensorflow==2.15.0